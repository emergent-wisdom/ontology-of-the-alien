# Analysis and taxonomy-graph dependencies. These lower bounds are not an
# exact lockfile for the historical generation environment.
numpy>=1.24.0
networkx>=3.0
sentence-transformers>=2.2.0

# Required by sentence-transformers
torch>=2.0.0
